<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.segway.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionLayout">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnimatorCompareActivity"
            android:label="@string/label1" />
        <activity
            android:name=".TransitionGo"
            android:label="@string/label2" />
        <activity
            android:name=".ConstraintSetDemo"
            android:label="@string/label3" />
        <activity android:name=".MotionLayoutDemo" />
        <activity android:name=".MotionLayoutDemo2" />
        <activity android:name=".PathMotionArcActivity" />
        <activity android:name=".KeyFrameActivity"></activity>
    </application>

</manifest>